---
'kimaki': minor
---

Show `always` patterns in permission requests — when `permission.always` contains patterns, they are now displayed as an **Always Allowed:** line in the Discord permission message alongside the existing **Pattern:** line. Previously the always patterns were silently hidden, making it unclear which file patterns would be auto-approved going forward.
