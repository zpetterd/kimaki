{
  "name": "root",
  "private": true,
  "scripts": {
    "prepare": "pnpm -r --filter errore --filter libsqlproxy --filter opencode-injection-guard --filter traforo --filter fly-admin --filter profano --filter sigillo --filter discord-slack-bridge run build",
    "test": "NODE_ENV=test pnpm --filter kimaki run vitest",
    "dev": "pnpm --filter kimaki dev",
    "kimaki": "pnpm --filter kimaki play"
  },
  "devDependencies": {
    "oxfmt": "^0.24.0",
    "prettier": "^3.6.2",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vite": "^7.1.4",
    "vite-node": "^3.2.4",
    "vitest": "^3.2.4"
  },
  "repository": "https://github.com/remorses/",
  "author": "remorses <beats.by.morse@gmail.com>",
  "license": "",
  "dependencies": {
    "@opencode-ai/sdk": "^1.15.11",
    "string-dedent": "^3.0.2",
    "tiny-jsonc": "^1.0.2"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@discordjs/opus"
    ],
    "overrides": {
      "spiceflow": "1.26.0-rsc.3"
    }
  }
}
